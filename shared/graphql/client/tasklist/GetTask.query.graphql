query GetTask($taskId: ID!) {
  getTaskList(id: $taskId) {
    createdAt
    id
    progress
    title
    users {
      avatar
      name
      email
      id
    }
    todos {
      ...SimpleTodo
    }
  }
}