mutation AssignUserToToDo($todoId: ID!, $userId: ID!) {
  assignUserToToDo(todoId: $todoId, userId: $userId) {
    assignees {
      id
      email
      avatar
    }
    content
    isCompleted
  }
}